/** Apply */
type Apply = unknown;

/** Push an element to a tuple */
type Push = unknown;

/** Filter a tuple */
type Filter = unknown;

/** Determine if the given type extends another */
type Extends = unknown;

/** Apply an operation to all inputs */
type ApplyAll = unknown;

/** Capitalize a string */
type Cap = unknown;